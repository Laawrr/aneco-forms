DATABASE_URL="mysql://roots:roots@192.168.137.1:3306/aneco_forms"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Secrets for signing tokens (use long random strings) Using this command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_TOKEN_SECRET=6aeca5326ab8113f4064304790e8baa531138950446f1d7a026374e4e2a6aef0
# Access token lifetime (string supported by jose): e.g. "15m"
JWT_ACCESS_TOKEN_EXPIRES=15m
# Refresh token lifetime in days
REFRESH_TOKEN_EXPIRES_DAYS=30

# Optional: adjust bcrypt salt rounds
BCRYPT_SALT_ROUNDS=10